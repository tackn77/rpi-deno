pipeline {
  agent {
    dockerfile {
      filename 'Dockerfle'
    }

  }
  stages {
    stage('') {
      steps {
        build 'docker build -c 2 Dockerfile'
        timestamps()
      }
    }

  }
}